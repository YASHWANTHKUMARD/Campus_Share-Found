<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MATBASE">

        <!-- Main Entry Point: Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Feature Activities -->
        <activity
            android:name=".MaterialSharingActivity"
            android:exported="false"
            android:label="Share Items" />
        
        <activity
            android:name=".PostLostActivity"
            android:exported="false"
            android:label="Post Lost Item" />

        <activity
            android:name=".ViewFoundActivity"
            android:exported="false"
            android:label="Found Items" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
            
    </application>

</manifest>
